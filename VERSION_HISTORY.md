# Version history

**v1.24 — Homepage redesign Phase 7 (final): interface & hero polish** (current)
- **1. Hero photo — selection and treatment**:
  - Selection: the featured-park pick (`app/page.tsx`) previously considered only seasonal timing (`bestMonths.includes(currentMonth)`, first match wins). Added a secondary preference — not a hard requirement — for a hotspot whose real photo shows wildlife/fauna over one that's pure landscape, applied only when more than one hotspot qualifies for the month. The check (`photoShowsFauna()`) looks for one of that hotspot's own listed `mainSpecies`/`birdSpecies` names appearing in its photo's real Wikimedia file title (e.g. "Bengal tiger in Bandhavgarh National Park..." vs "The Main Lake at Ranthambore National Park..." — Ranthambore's own title has no species name, so it wouldn't win the tie-break over a hotspot whose title does) — not an invented generic keyword list. If no qualifying candidate's photo mentions its own species, the original seasonal order is left untouched. As of this build only July matches (Valley of Flowers alone), so this tie-break currently has zero effect — confirmed by checking `bestMonths` matches per month before writing the logic, per the phase brief's own instruction not to force a worse seasonal match for a better photo.
  - Treatment: added a slow Ken Burns zoom/pan (`.hero-ken-burns`, `app/globals.css`) — 55s, `scale(1)→scale(1.08)` with a slight diagonal pan, `ease-in-out infinite alternate`. Wrapped in `@media (prefers-reduced-motion: no-preference)` rather than gated by JS/`matchMedia` state, so reduced-motion users never register any animation at all (static crop) with no extra client-side detection code needed — applied to the photo layer in both `LandedDescent` and `ScrollDescent` (`components/DescentSequence.tsx`).
  - Gradient audit: `LandedDescent`'s existing vignette (a 100deg gradient, darkest at the left where the text column sits, fading toward the right where photo detail should read through) was already doing real editorial work, not just uniformly darkening the image — verified, no change needed there. `ScrollDescent`'s hero photo, however, used a different, generic bottom-heavy `to-t` vignette with no relationship to the left-aligned text position — replaced it with the exact same 100deg gradient `LandedDescent` uses, so the animated and landed hero states share one consistent, actually-targeted vignette instead of two different ad-hoc ones.
- **2. Search bar restyle**: `components/SearchBar.tsx` gained an opt-in `variant?: "light" | "dark"` prop (default `"light"`, unchanged) rather than being restyled in place — this same component is also reused by `FilterPanel` on `/map` and `/hotspots`, both on light backgrounds, so a global restyle would have broken those. `DescentSequence`'s `HeroFunctional` now passes `variant="dark"`: translucent `bg-white/10` with a `border-white/25`, `backdrop-blur-md`, white text, `placeholder:text-white/50`, and a `ring-sand/50` focus ring — no functional change, confirmed by typing into it and reading the resulting value back. Every other `SearchBar` usage (`FilterPanel`) is byte-identical to before.
- **3. Photo credits**: `components/HotspotImage.tsx`'s credit badge is now quiet-but-present by default (`opacity-70`, smaller `text-[9px]`, lower-contrast `bg-black/30`/`text-white/60`) rather than fully invisible, brightening to full contrast (`opacity-100`, `bg-black/60`, white text, a visible focus ring) on hover **or** keyboard focus. Fixed a real accessibility bug found while wiring this up: the badge previously had `tabIndex={-1}`, which explicitly removes an element from the tab order — meaning keyboard users could never reach or activate it at all, regardless of any visual treatment. Changed to `tabIndex={0}` with a real `onKeyDown` handler (Enter/Space) alongside the existing click handler; stayed a `<span role="link">` rather than becoming a real `<a>`, since this component is nested inside a clickable `<Link>` in several call sites (mosaic tiles, `HotspotCard`) and a real anchor can't validly nest inside another anchor. Also removed `showCredit={false}` from both of `DescentSequence`'s hero photo calls — the hero photo previously had **no credit at all**, a real licensing-compliance gap now closed; every other `HotspotImage` call site (mosaic tiles, cards, detail pages) already had credit on and is otherwise unchanged.
- **4. Typography scale**: increased the *contrast* between the hero and everything else rather than bumping every heading uniformly. Hero `<h1>` (`DescentSequence`'s `HeroCopy`): `text-6xl sm:text-7xl leading-[0.98]` → `text-7xl sm:text-8xl leading-[0.9]`, with more surrounding space (`mt-6`→`mt-8` above and below). The three supporting headings — Field Notes' "Where to go in {month}", "Explore by experience", and "How we verify"'s "Verified, not guessed." — were previously an inconsistent mix (`text-3xl font-black`, `text-3xl font-black`, `text-4xl font-semibold`); normalized all three to the same `text-3xl font-semibold`, so the page reads as a clean two-tier hierarchy (one dramatically large hero display headline, then calm uniform supporting headings) instead of several headings quietly competing with each other for attention.
- **5. Mosaic tile variation**: `experienceTiles` (`app/page.tsx`) now carries a computed `size: "big" | "wide" | "normal"` field — ranked by each tile's own real `parkCount` (already shown in its caption), not guessed: the highest-count tag (Photography, 33 parks) gets a 2×2 `"big"` tile, the second-highest (Offbeat, 24 parks) gets a 2×1 `"wide"` tile, the rest stay normal single cells. The variation breakpoint moved from `lg:` (1024px) down to `md:` (768px) so it's visible on more real desktop widths, not only very wide viewports; below `md:` every tile stacks as a normal single/double-width cell in the existing 2-col (mobile) / 3-col (`sm:`) grid, unchanged responsive behavior.
- Left all Field Notes and trust-callout **copy and data logic** exactly as the last two phases built them — every change in this phase is visual/interaction polish only, per the phase brief's constraint.
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Reduced-motion tested with real rigor, not a workaround this time**: this browser environment happens to have `prefers-reduced-motion: reduce` set at the OS level, so `getComputedStyle()` on `.hero-ken-burns` showed `animationName: "none"` — genuine confirmation of the disabled path, not a simulation. Then temporarily force-applied the animation via an injected `!important` override (reverted immediately after) to confirm the `heroKenBurns` keyframe itself is correctly defined (55s, `alternate`, `infinite`) when the media query does allow it — verifying both branches of the same CSS rule, not just one.
- **Also verified in the browser**: real keyboard Tab navigation (not just programmatic `.focus()`, which doesn't reliably trigger `:focus-visible`) reaching the hero photo credit and confirming `opacity: 1` + a visible focus ring; the dark-glass search bar accepting real typed input; the mosaic's big/wide tiles rendering correctly at a standard desktop width; `/hotspots`' `FilterPanel` search bar and `HotspotCard` photo credits confirmed unchanged from this phase's `HotspotImage`/`SearchBar` edits; both light and dark site themes checked on the "Explore by experience" and "How we verify" sections

**v1.23 — Homepage redesign Phase 6: the trust callout**
- New closing section in `app/page.tsx`, between "Explore by experience" and the footer — the final piece of the homepage redesign plan. Deliberately quiet after the photo-heavy mosaic above it: no card/border treatment, no photo, centered in a narrow `max-w-2xl` column with generous `py-20 sm:py-24` whitespace, closing the page on restraint rather than another visual peak
- **Exact copy**:
  - Eyebrow: "How we verify" (`font-mono text-xs uppercase tracking-[0.2em] text-river` — same mono-utility-label idiom as "Monsoon field notes · Valley of Flowers" elsewhere on the page, and reuses `text-river`, the same color the `/data-sources` page's own eyebrow already uses, rather than introducing a new accent color for this role)
  - Headline: "Verified, not guessed." (serif, `text-4xl font-semibold` — intentionally `font-semibold` rather than the `font-black` used for section headers like "Explore by experience" elsewhere on this page, so it reads as a calmer closing statement than another section heading)
  - Body: "{N} of our 42 hotspots carry a closure note backed by a named forest department or a well-documented pattern — not a guess — and {M} carry an explicit disclosure when the real complexity is jurisdiction or access, not season. Species presence data comes from real eBird and iNaturalist records, not invented lists."
  - Link: "See how we verify every park →" to `/data-sources`, styled with dark text and hover-underline — the amberfield color is used sparingly, only on the arrow character itself, not the link text or a button fill, per the phase brief's "sparingly" instruction
- **Every number is computed from real data at build time, not estimated**: `OFFICIAL_OR_INFERRED_COUNT` = `Object.values(closureInfo).filter(c => c.confidence === "official" || c.confidence === "inferred").length` from `data/closures.ts` (currently 28 of 42), `STRUCTURAL_RISK_COUNT` = `Object.keys(structuralRisks).length` from `data/structuralRisks.ts` (currently 8), and `hotspots.length` (42) — all three recompute automatically as those data files grow, so this copy can't silently go stale. The one non-computed claim ("eBird and iNaturalist") is a stable structural fact about *how* species data is sourced (confirmed against the existing `EbirdChecklist`/`ExtendedSpeciesProfile` components and their `source` field), not a count, so it's stated directly rather than derived — exactly the kind of fact the phase brief said was fine to state plainly
- **Not tied to the monthly pick**: this section reads `closureInfo`/`structuralRisks`/`hotspots` directly (all static imports), with no dependency on `featured`, `now`, or any other per-visit/per-month state — it renders identically regardless of which park is featured elsewhere on the page, and sits outside the `{featured && ...}` conditional that wraps the Descent sequence and Field Notes sections
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Verified in the browser**: the section renders with the real computed numbers (28, 42, 8) in a fresh tab; clicked the link and confirmed it navigates to `/data-sources`; toggled dark mode and confirmed the headline, body text, and eyebrow all adapt correctly (the headline via the existing global `html.dark h1,h2,h3` color override, no new dark-mode CSS needed)

**v1.22 — Homepage redesign Phase 5: photo-tile mosaic for "Explore by experience"**
- **Confirmed the real experience tag set before building anything**: grepped every `experienceTags` entry in `data/hotspots.ts` rather than trusting the old section's hardcoded labels — the real set is `Photography`, `Safari`, `Birding`, `Trekking`, `Family-friendly`, `Offbeat` (6 tags, matching `FilterPanel`'s own `experience` dropdown exactly). The old section's five labels (`Wildlife Photography`, `Monsoon Forests`, `Rare Species`, `Family Nature Trips`, etc.) didn't match any real tag value at all — they were invented copy, not derived from data
- **Representative-hotspot selection, made deterministic and documented** (`app/page.tsx`'s `buildExperienceTiles()`): for each tag, filters to hotspots carrying that tag **and** having a real photo in `data/hotspot-images.json`, then picks the first one (in `hotspots.ts`'s own declaration order) not already used by an earlier tile. The "not already used" rule is a deliberate refinement beyond the brief's literal suggestions: a first attempt at "pick the hotspot with the most matching tags" put Jim Corbett National Park (which alone carries 4 of the 6 tags) on four of the six tiles — technically deterministic, but a repetitive, boring mosaic that defeated the point of a photo index. First-unused-match produces six distinct parks/photos instead: **Photography → Jim Corbett, Safari → Ranthambore, Birding → Kaziranga, Family-friendly → Kanha, Offbeat → Tadoba Andhari, Trekking → Hemis**
- **Zero tags had missing photos**: all 42 hotspots in this dataset have a `hotspot-images.json` entry, so every one of the 6 tiles found a real qualifying photo — the `HotspotVisual` gradient/icon fallback path exists in the tile code (same `HotspotImage` component, same automatic fallback used everywhere else) but wasn't exercised for any of today's 6 tiles. Verified it still renders correctly in this new tile layout by temporarily forcing a nonexistent slug into one tile (reverted after) — the gradient fills the tile edge-to-edge exactly like a real photo would
- **Tile redesign**: real `HotspotImage` fills each tile, a bottom `bg-gradient-to-t` scrim, the tag name overlaid in the serif display face, `group-hover:scale-110` zoom transition (clipped by the tile's own `overflow-hidden`, not the image's, so the credit badge doesn't distort). Removed the icon circles and the single repeated boilerplate sentence ("Find locations, seasons, and planning notes matched to this travel style") entirely — no per-tile copy was hand-written; the caption is `"{parkCount} parks · Featuring {hotspot.name}"`, composed the same way Field Notes' `bestForSentence()` already composes from real fields, where `parkCount` is the actual count of hotspots carrying that `experienceTags` value (not the photographed subset, not invented)
- **Mosaic layout**: `grid-cols-2 sm:grid-cols-3`, switching to `lg:auto-rows-[210px] lg:grid-flow-dense` at desktop with the Photography tile (highest `parkCount`, 33) given `lg:col-span-2 lg:row-span-2` — one large flagship tile plus five smaller ones, dense auto-flow backfilling around it rather than manually placed grid-template-areas. Below `lg` the grid is a plain 2/3-column grid (Photography's tile spans the full 2-column width on mobile too, reading as a natural hero-first stack), matching the codebase's existing mobile-stacks/desktop-grid convention
- **Routing reused, not rebuilt**: checked `/map` and `/hotspots` first — `/hotspots` has no URL-synced filters at all (local `useState` only), and `/map` only reads `?query=` and `?season=` from the URL, no structured `?experience=` param. Since `filterHotspots()`'s free-text search already concatenates `experienceTags` into its searchable haystack, `/map?query={tag}` was already a working experience filter — the same mechanism the old section's cards already used. Reusing it means the click-through result count can be slightly broader than the tile's own `parkCount` caption (e.g. "Safari" tile says 21 parks — the real strict-tag count — but `/map?query=Safari` returns 24, since free-text search also matches the word "safari" anywhere else in a hotspot's fields); this is an honest, inherent consequence of reusing the existing broad-search mechanism rather than building new exact-tag routing, not a fabricated or inconsistent number
- Photo credit is **on** for every tile (`HotspotImage`'s default `showCredit`, not suppressed), with `group` on each tile's outer `<a>` so the existing hover-reveal credit overlay works exactly as it does elsewhere in the codebase
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Verified in the browser**: all 6 tiles render with correct distinct photos/captions in a fresh tab (a stale/reused browser tab briefly showed the gradient scrim as fully opaque — confirmed via `elementsFromPoint` and computed-style inspection that the gradient CSS itself was mathematically correct and a clean tab reload rendered normally, so this was a dev-server/tab rendering artifact, not a code bug); clicked the "Safari" tile and confirmed it navigates to `/map?query=Safari` with real filtered results; temporarily forced a nonexistent slug to confirm the `HotspotVisual` fallback renders intentionally (reverted after)

**v1.21 — Homepage redesign Phase 4: split-screen Field Notes**
- Replaced the small square thumbnail next to the Field Notes heading with a real two-column split-screen at desktop widths (`app/page.tsx`): `HotspotImage` fills a `42%`-width column at full section height (`lg:grid-cols-[42%_1fr] lg:items-stretch`), the eyebrow/heading/cards fill the rest. Below `lg`, the two columns are plain stacked block elements (no grid/flex applied until `lg:`), so photo-above-text stacking on mobile/tablet comes from ordinary document flow rather than extra responsive rules — same breakpoint convention already used for the hotspot detail page's `lg:grid-cols-[1fr_360px]` sidebar split
- **Distinct crop, same source photo**: per the phase brief, there's only one real photo per hotspot (`data/hotspot-images.json`), so this section deliberately doesn't try to source a second image. The panel gets a genuinely different composition than the hero's wide landscape crop purely from its own aspect ratio — a narrow, tall column (`h-64`/`sm:h-80` on mobile, full stretched section height at `lg:`) makes `object-cover` show a much tighter, more vertical slice of the same photo. No new `HotspotImage` prop or object-position override was needed
- **HotspotVisual fallback confirmed graceful**: `HotspotImage` already falls back to the illustrated gradient+icon (`HotspotVisual`) when a hotspot has no photo entry; since both components pass `className` straight through to their own outer `<div>`, the split-screen sizing classes apply identically either way — verified by temporarily forcing a nonexistent slug (reverted after) and confirming the gradient fills the full panel and reads as intentional, not broken
- **Cards refined into marginalia, not alert boxes**: replaced the `rounded-sm border border-white/15 bg-white/10 p-4 backdrop-blur-md` boxed treatment with the site's existing pull-quote idiom (`border-l-2 {accent-color}/40 pl-4`, already used for hero/detail-page summary lines) — a thin colored accent rule instead of a full bordered box, more line-height and breathing room (`leading-6`), and a lighter-weight label (`text-[11px] font-semibold tracking-[0.15em]` and 14px icons, down from `text-xs font-bold tracking-wide` and 15px icons). Kept every card's data logic completely unchanged — same `bestForSentence()` composition, same real `closureInfo`/`structuralRisks` lookups, same 2-vs-3-column fallback when a park has no structural risk entry (moved to the `lg:` breakpoint instead of `sm:`, since the cards now live inside the narrower text column rather than a full-width section)
- **Photo credit preserved**: `HotspotImage`'s existing hover-reveal author/license credit (`group-hover:opacity-100`) needs an ancestor with the literal `group` class to work — added `group` directly to the `className` passed into `HotspotImage` (previously this call used `showCredit={false}`, now credit is on and functional). Confirmed by hovering the panel and seeing the real "Photo: {author} · {license}" caption appear, unclipped, at the panel's true bottom-right corner regardless of crop
- The "canopy dark" (`bg-forest-900`) section background from the last fix still applies correctly to the text side; the photo side needs no background of its own since the image (or its gradient fallback) fills that column edge-to-edge
- **Data logic untouched**: this phase is layout only — the Best For/Know Before You Go/Travel Caution composition and conditionals are byte-identical to the last fix, just re-arranged into the new column structure
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Verified in the browser**: the real current pick (Valley of Flowers) in the 2-column (no structural risk) layout; temporarily swapped in Sundarbans National Park (reverted after) to confirm the 3-column layout with a real structural-risk card renders correctly; temporarily forced a nonexistent slug (reverted after) to confirm the `HotspotVisual` fallback fills the panel intentionally; confirmed the photo credit overlay appears on hover, unclipped. Window-resize testing for the mobile stacked layout wasn't reliable in this remote browser tool (the CDP viewport didn't actually narrow), so that path is verified by CSS reasoning instead — `lg:grid` only activates at 1024px+, and the two panel `<div>`s have no competing layout classes below that breakpoint, so they stack via ordinary block-level flow, the same guarantee the codebase already relies on for its other `lg:grid-cols-[...]` splits

**v1.20 — Homepage redesign Phase 3: fix the fused landing**
- **Issue 1 — removed the redundant "This month's pick — {park} · Verified {date}" badge line** from the hero (`components/DescentSequence.tsx`'s `HeroCopy`), since the continuation section below now properly names and explains the featured park itself. Rest of the hero (eyebrow, headline, tagline, stat line, CTAs, search, chips) is untouched. Cleaned up the props/imports that line alone needed (`closure`, `ConfidenceDot`, `formatVerifiedDate`, `closureInfo`/`ClosureInfo` were all removed from `DescentSequence.tsx` since nothing else there used them)
- **Issue 2 — Field Notes made genuinely park-specific**, composed from real per-park data instead of `data/seasonalWisdom.ts`'s season-only copy (which previously showed things like "core zones of most tiger reserves close" under parks that aren't tiger reserves):
  - **Best for**: a sentence generated from the featured park's own `data/ecosystems.ts` ecosystem type + up to 2 of its `mainSpecies`/`birdSpecies` (`data/hotspots.ts`) + the fact that the current month is genuinely one of its `bestMonths` — e.g. "Valley of Flowers is an alpine habitat, known for Blue Sheep and Himalayan Weasel. July is one of its best months to visit." No invented claims beyond what those fields already state; a small `bestForSentence()` helper composes it in `app/page.tsx`, not hand-written prose
  - **"Avoid" → "Know before you go"**: now `closureInfo[featured.slug].note` verbatim (not paraphrased), plus a small mono "Source: {sourceName}" caption when the closure entry has one — same field FreshnessBadge already surfaces elsewhere, just presented as a caption here instead of a popover
  - **Travel caution**: now only renders when `data/structuralRisks.ts` has an entry for the featured park, using that entry's real `summary` — no generic fallback. When absent (true for most parks, including this month's Valley of Flowers), the card is omitted entirely and the grid drops from 3 columns to 2 (`sm:grid-cols-3` → `sm:grid-cols-2`), rather than leaving a hole or fabricating content
  - `data/seasonalWisdom.ts` was **not** touched or removed — confirmed it's still used by `app/hotspots/[slug]/page.tsx`'s "Season & closures" fallback (for parks with no `closureInfo` entry) and left that usage exactly as-is. Only the homepage's own dependency on it (`monthToSeason` import stays since `currentSeason` still drives the "Where to go this month" CTA link and the eyebrow text; the `wisdom`/`seasonalWisdom` variable itself was removed) — verified via `grep` before touching anything
- **Issue 3 — removed the duplicate/cut-off full-bleed photo** in the continuation section: it's now a solid `bg-forest-900` (canopy dark) section with no background image at all, so it doesn't repeat the hero's photo a second time in an awkward crop. Added a small explicitly-sized `HotspotImage` thumbnail (64–80px, `rounded-sm`) next to the section's heading instead — a contained supporting image, not a second full-bleed treatment. The "Know before you go"/"Best for"/"Travel caution" cards keep their existing translucent `bg-white/10` treatment (still reads correctly against a flat dark background, not just a photo one)
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Verified in the browser**: the real current pick (Valley of Flowers, July) — confirmed the hero no longer shows the pick badge, the continuation section shows its own real `ecosystem`/species-composed sentence and real closure note with correct 2-column layout (no structural risk entry for this park), and the section background is solid dark with only a small thumbnail, no repeated photo. Also temporarily swapped the featured park to Sundarbans National Park (which does have a `structuralRisks` entry) to confirm the 3-column layout renders correctly with a real "Sundarbans sits under overlapping Forest Department, Biosphere Reserve, and Tiger Reserve jurisdiction" caution card, then reverted. Caught and fixed a real "a alpine habitat" → "an alpine habitat" article-agreement bug in `bestForSentence()` during this pass. Confirmed `data/seasonalWisdom.ts`'s other usage (hotspot detail page's closure fallback) still renders correctly, untouched

**v1.19 — Homepage redesign Phase 2: fuse hero into the Descent landing**
- Restructures what happens at the end of Phase 1's Descent sequence and removes the two sections that used to follow it, so the homepage top is one continuous story (descent → landed hero → same-park continuation) instead of three separate beats that repeated the same monthly pick
- **Hero content moved into `DescentSequence`, not duplicated**: the old hero `<section>` in `app/page.tsx` (eyebrow, "Explore India's Wild Side" headline, tagline, stat line, both CTAs, search bar, tag chips) is gone entirely — removed, not left in place alongside the new content. Its exact copy/markup now lives in two shared subcomponents inside `components/DescentSequence.tsx` (`HeroCopy` for eyebrow/headline/tagline/stat-line/CTAs, `HeroFunctional` for the search bar + chips), reused by every rendering path so the copy exists in one place
  - One deliberate restructuring, not a copy change: the descent's existing "This month's pick" line was previously its own large centered `<h2>`; it's now a smaller inline badge ("This month's pick — {park} · Verified {date}") sitting under the tagline, so it doesn't compete with the site's own headline for the primary-heading role now that both appear in the same moment
- **New final beat**: once scroll progress passes 0.85 (of the zoom range), the search bar and chips pop in with a quick `easeOutBack` scale+opacity transition — a distinct beat after the rest of the hero copy has already landed (its own fade phase ends at 0.96). Chose a scroll-progress trigger over a scroll-idle/dwell timer so the whole sequence stays on one consistent model (everything else is already scroll-linked) rather than mixing in a timer
- **Repeat-visit and reduced-motion now render the same landed hero immediately**, sharing one `LandedDescent` component (previously Phase 1's `StaticDescent`) — not two separate builds. This required a gating change from Phase 1: since the hero content now lives entirely inside `DescentSequence`, "already seen" can no longer mean "render nothing" (there'd be no hero left on the page). Both the server render and the pre-hydration client render now default to `LandedDescent` (assume seen), and only a confirmed true-first-visit-with-motion-allowed upgrades to the tall animated `ScrollDescent` after a `useLayoutEffect` check — the opposite default from Phase 1, and a better one: the common case (repeat visits) never flashes empty content, only the rare true-first-visit briefly shows the landed hero for one frame before upgrading
- **Field Notes 3-card grid replaced** with a full-bleed continuation section for the *same* featured park (`app/page.tsx`), not a different pick: reuses `HotspotImage` for that park's photo as the section background, and carries the exact same Best For/Avoid/Travel Caution copy and icons as annotated marginalia cards over it (necessary color-only adaptation: labels/text recolored for a dark photo backdrop — `text-forest-700`→`text-forest-300`, `text-forest-900`→`text-white`, `field-card`→translucent `bg-white/10` cards — using the same "badge on dark photo" idiom already established elsewhere on the site, e.g. `IndiaMap`'s legend chips)
- **"Good bets this month" kept, not dropped or duplicated**: reviewed for redundancy with the descent's own pick and judged it still earns its place — `best[0]` (the featured park) already gets a full treatment in the descent + continuation section, so showing it a third time in a card grid would be pure repetition, but `best.slice(1)` (other parks matching this month) is genuinely new information the descent doesn't cover. Renamed "Good bets this month" → "Other good bets this month" (necessary copy tweak given it's now explicitly the *other* options) and the section only renders when there are any (`otherBets.length > 0`) — this month there's only one match, so it currently doesn't render, and that's correct, not a bug
- `app/page.tsx`'s "Explore by experience" section at the bottom is untouched, as is `/map`
- Cleaned up now-unused imports in `app/page.tsx` (`useRouter`, `useState`, `SearchBar`, `IndiaMap`, `ArrowRight` all moved to or no longer needed since their only use was the removed hero); kept the `"use client"` directive despite no longer using any hooks directly, since `now = new Date()` still needs to evaluate fresh in the browser on every load for the featured park to stay genuinely live/seasonal rather than frozen at build time — documented with a one-line comment
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Tested in the browser**, all three paths: repeat-visit (set `wia-descent-seen=1`, reloaded — landed hero present immediately, `document.body.scrollHeight` confirmed no added scroll height); reduced-motion (temporarily forced the branch, same result — landed hero immediately, `seen` flag marked on mount, reverted after); first-visit animated (cleared the flag, scrolled the full sequence — confirmed the fused hero copy fades in together with the photo/park-name/verified-badge, then the search bar/chips visibly pop in a beat later once scroll passed ~0.85, then a clean handoff into the same park's continuation section). Also clicked a header nav link partway through the animated sequence to confirm the fixed/`z-50` header stays clickable throughout, unchanged from Phase 1

**v1.18 — Homepage redesign Phase 1: "The Descent" entry sequence**
- New client component `components/DescentSequence.tsx`, inserted at the very top of `app/page.tsx` immediately before the existing hero `<section>` — the hero itself, and every other section on the page, is byte-identical to before
- **Mechanic**: a ~320vh tall container with a `sticky top-0 h-screen` inner stage. An `rAF`-throttled scroll listener computes progress 0–1 from the container's `getBoundingClientRect()` against `window.innerHeight`, which drives four overlapping beats: the map zooming/panning from a full-country view toward the featured park's real projected coordinates (0–0.5), a darkening overlay (0.15–0.55), a crossfade into the park's real photo (0.4–0.78), and the reveal text rising in (0.68–0.96); an initial "Scroll to begin" cue fades out over the first 0.12. Once the container's height is exhausted the sticky stage unpins naturally and normal scrolling reveals the real hero underneath — no extra handoff logic needed
- **Reused real infrastructure, not rebuilt**: exported `projection`, `pathGen`, `VIEW_W`, `VIEW_H` from `components/IndiaMap.tsx` (previously module-private) so the Descent's map uses the exact same d3-geo Mercator projection and GeoJSON as the real map elsewhere on the site, rather than a second stylized shape; the featured park is `best[0]` from `app/page.tsx`'s existing "best this month" computation (no new selection logic); the photo layer is `components/HotspotImage.tsx`; the "Verified {date}" reveal text uses `data/closures.ts` + `formatVerifiedDate` from `lib/formatDate.ts` (same data source as `FreshnessBadge`), plus `ConfidenceDot` reused directly from `components/FreshnessBadge.tsx`. If there's no featured park this month (`best` empty) or no closure record for it, the sequence/badge simply omits rather than fabricating
- **First-visit-only gating**: checks `localStorage.getItem("wia-descent-seen")` in a `useEffect` after mount (SSR and first paint always render nothing, so there's no hydration mismatch). If already set, the component renders nothing and the page goes straight to the hero with no added height. If not set, it renders the sequence and marks the flag — for the full scroll sequence, as soon as it mounts (a mid-scroll refresh shouldn't force a second playthrough); for the reduced-motion fallback, immediately on mount since there's no "played to completion" moment to wait for. Tradeoff: a one-tick layout shift the very first time a browser loads the homepage, since the localStorage check can only happen client-side — acceptable for a first-visit-only flourish
- **Reduced motion**: `window.matchMedia('(prefers-reduced-motion: reduce)')` checked alongside the localStorage gate. When true, the tall scroll-jacked container is never rendered at all — instead a short, static `min-h-[60vh]` section with the same real photo, park name, and verified badge, no scroll-linked animation
- **Nav visibility**: `Header` is a shared layout element (`app/layout.tsx`, rendered once, before `{children}`) that's `position: fixed` with `z-50` — confirmed both the tall scroll stage and the static fallback render at default stacking (`z-index: auto`), so the header floats above the Descent's dark stage the entire time. A visitor can click Map/Hotspots/Species/About immediately without scrolling through any of the sequence
- **Bug found and fixed during verification**: the photo layer initially rendered as a flat solid color instead of the real image. Root cause — `position: relative` without an explicit `z-index` does not establish a new CSS stacking context, so the `-z-20` photo layer was escaping past its own section and losing to sibling elements (the adjacent hero section uses the same negative-z-index trick for its own map/photo layers). Fixed by adding `isolate` to the Descent's own section/stage container, which correctly contains its negative-z-index children — confirmed via `elementsFromPoint` before and after, and the existing hero/hotspot/species detail pages (which each have only one such section, no negative-z-index sibling immediately before them) were never affected by this and needed no change
- `npm run typecheck` and `npm run build` both pass clean (1408 static pages generated)
- **Tested in the browser**: first-visit path (cleared `wia-descent-seen`, scrolled through all four beats, confirmed the real "Valley of Flowers" photo/name/verified-date and clean handoff into the unchanged hero); repeat-visit path (reloaded with the flag set, confirmed it skips straight to the hero with no added height, twice — once before and once after the isolate fix); reduced-motion fallback (temporarily forced the branch to verify the static layout renders the real photo/badge correctly, since this sandbox has no way to emulate `prefers-reduced-motion` at the OS/CDP level — reverted immediately after)

**v1.17 — Texture the map canvas**
- `components/IndiaMap.tsx`'s ocean/background area was the one surface still completely flat, unlike the hero and species/hotspot fallback illustrations, which all already use the `.canopy-texture` dot pattern. Added a `.canopy-texture` layer directly behind the map's `<svg>`, covering the full canvas
- Because the landmass polygons (India's states plus neighboring countries) are opaque and render on top, the texture only actually shows through the ocean — it's invisible on land, exactly as intended, with zero changes to how the land itself is drawn
- Landmass state fills were deliberately **left untouched** — re-read the existing code comment explaining why `regionFill`/state fill is kept flat (so it doesn't visually compete with the ecosystem-pin colors, both of which lean on green/orange/blue); adding contour linework there risked exactly the kind of visual competition that comment warns against, so per the phase brief's own fallback guidance, this phase textures the ocean only
- Nothing else changed: ecosystem pin colors/logic, region-cluster fill colors, the preview card, access-point rendering, legend/layer-toggle behavior, and the zoom/pan transition are all byte-identical to before
- Verified in the browser at both zoom levels: full-country view (dot pattern clearly visible in open ocean, stops cleanly at the coastline) and a zoomed-in region view (Ladakh/Kashmir — labels, pins, and legend all remain fully legible); also checked the hero, where the texture reads as a faint, appropriately subtle layer under the hero's own dark gradient overlay
- Noticed a pre-existing, unrelated React hydration warning in `IndiaMap.tsx` (a floating-point rounding difference in a marker's `transform` coordinate, line 303) while testing — not touched by this phase, flagged separately for a future fix

**v1.16 — Replace flare accent with sand/amberfield gold**
- Swapped every use of the lime-green `flare` Tailwind color (#d7e64c) for the warmer sand/amberfield gold family (#d8b56d / #d98c2b) — the site's premium-editorial direction, not a lime tech-startup one. Found and replaced **25 usages across 13 files**, all on dark or dark-overlaid backgrounds (hero, header, map, cards):
  - **Solid fills → `amberfield`** (5 instances): the hero's "Open Wildlife Map" CTA, the map hotspot-preview-card "View details" CTA, and 3 map-legend badges (`IndiaMap.tsx`'s active-layer-count circle, active layer-toggle chips)
  - **Text/border/translucent accents on dark → `sand`** (19 instances): the hero eyebrow line + its underline dash + the "Wild Side" headline emphasis + the chip-hover state; header nav hover states (desktop + mobile) and the theme-toggle hover; the "back to map"/"back to species guide" links and their border-l-2 accent bars on all 3 hero-style detail pages (hotspot, Flagship species, Extended species); the small forest-900/90-pill category/type badges on `HotspotCard`, `SpeciesCard`, and `MergedSpeciesCard`; `HotspotPreviewCard`'s state/region text, type badge, and permits link (both docked and grid variants); and `IndiaMap`'s "Best in {month}" translucent badge and the two hover-tooltip labels
  - **1 special case**: `SpecialityBadges`'s Iconic badge already used `amberfield` for light-mode text — its `bg-flare/20` tint became `bg-amberfield/20` (matching the text color it's paired with) and its `dark:text-flare` became `dark:text-sand`
- Checked contrast rather than assuming the old pairings still worked: `text-forest-900` on `amberfield` fills is ≈6.2:1 (still clearly passes WCAG AA, down from flare's ≈12.8:1 but with plenty of margin — kept as-is, no text-color change needed), and `text-sand` on the dark forest-toned backgrounds it now sits on is ≈8.8:1 (comfortably legible; sand worked everywhere, no fallback to amberfield needed for text)
- `tailwind.config.ts`'s `flare` color definition itself was deliberately left in place per the phase brief. Confirmed afterward: nothing in `app/`, `components/`, or `data/` references `flare` anymore — the only remaining match in the whole repo is that one definition
- Verified in the browser (both themes' hero, the map's legend/badges, and a hotspot detail page): headline emphasis and CTA button both clearly legible against the dark forest background; no layout, copy, or `IndiaMap` structural changes — color-substitution only

**v1.15 — Formalize the design token system**
- New named CSS custom properties in `app/globals.css`: `--color-canopy`/`--color-canopy-2` (new deep-forest hues, for future dark-mode-first sections regardless of the site's own theme toggle), `--color-slate` and `--color-sandstone` (new, for future wetland/water and desert/dry-forest content), `--color-turmeric` (named after the existing `sand` gold accent, same hex, no new color introduced), and `--color-paper`/`--color-ink` (named after the existing light-mode background/text colors, flipped to the existing dark-mode values under `html.dark`)
- Purely additive naming this phase — no visual changes. Verified by comparing computed CSS values before/after in both light and dark mode (`body` color, `.field-card` background, and the new variables' resolved hex), not just a visual screenshot diff
- Replaced 5 exact-match hardcoded hex literals in `globals.css` with the new variables (`body`'s background/color and `.field-card`'s background, in both the light default and the `html.dark` override)
- Deliberately left unmapped, with reasons: `globals.css`'s secondary gradient stops (`#f4efe1`, `#edf4ef`, `#0b1410`, `#0a120d`) and the dark-mode heading/card colors (`#f3f6ef`, `#152119`) are close to but not exact matches for any new token; several `rgba(r,g,b,alpha)` overlay effects share their RGB triplet with a token (turmeric, moss, forest-700, flare) but converting them would need new RGB-component variables, out of scope this phase; `tailwind.config.ts`'s existing `sand`/`ivory` Tailwind theme colors are a parallel, already-working token mechanism for utility classes and weren't touched
- `components/IndiaMap.tsx` has three literal `#fbf7ec` values (SVG label/border colors) that exactly match `--color-paper`, but were **not** replaced: unlike the body/card usage, these map labels are meant to stay a constant light cream regardless of the site's light/dark toggle, and `--color-paper` flips to a dark hex under `html.dark` — swapping it in would have made map labels invisible in dark mode, a real regression this phase is explicitly meant to avoid
- `.canopy-texture` (the dot-pattern treatment) is already applied in three places: the homepage hero background (`app/page.tsx`), and as the fallback illustration backdrop in both `components/SpeciesVisual.tsx` and `components/HotspotVisual.tsx` — confirmed working, ready for later phases to lean on more

**v1.14 — Close the iNaturalist mapping gap**
- Reviewed the 10 hotspots left pending from v1.9's discovery pass against their already-fetched `data/inaturalist-candidates.json` data — no re-discovery needed. **5 confirmed, 5 left for further review:**
  - Confirmed: `hemis-national-park` (the one India-plausible candidate among three, and its unusually large bbox matches Hemis being India's largest national park), `panna-national-park` (picked the bare-name park-boundary candidate over a same-named `admin_level 20` town/district record that would have wrongly covered a much bigger area), `periyar-tiger-reserve` (picked the whole-reserve record over its two separate East/West forest-division records), `silent-valley-national-park` (picked the more specifically NP-named of two plausible same-park records), `valley-of-flowers` (picked the park-specific record over the much larger combined Nanda-Devi-plus-Valley-of-Flowers World Heritage Site record)
  - Still unmapped, same conservative bar as v1.9: `desert-national-park` and `manas-national-park` (their only candidates are still Australia's/Zimbabwe's same-named parks), `gir-national-park` and `kuno-national-park` (every candidate is an unrelated international place that merely starts with the same letters — none are in India), `thattekad-bird-sanctuary` (still just an ambiguous forest-division record, not clearly the sanctuary's own boundary)
- Added **radius fallbacks** for the 5 hotspots with no iNaturalist Place at all, sized per park rather than one blanket number: Chilika Lake 20km (India's largest brackish lagoon, ~1,100 km²), Little Rann of Kutch 25km (deliberately capped well below its real ~4,950 km² rather than trying to cover it all), Melghat Tiger Reserve 20km (~2,000+ km² core and buffer), Sariska and Tadoba Andhari 15km each (moderate ~600–900 km² core zones)
- Ran `npm run fetch:inaturalist` for all 10 newly-mapped hotspots: **1,340 species records across 33 places** (up from 915/23) — Mammalia 617, Reptilia 519, Amphibia 204. 2 places still return zero species (Simlipal, Keibul Lamjao, unchanged from v1.10). One transient fetch failure on an already-confirmed hotspot (Bandhavgarh) left its existing 72-species record untouched, per the fetch script's existing incremental-write behavior
- Net effect on the Extended species tier: **1,359 canonical species, up from 1,289** (verified live — e.g. Hemis's Woolly Hare now has its own Extended profile, correctly linking back to Hemis National Park)
- The 4 hotspots already explained by `data/structuralRisks.ts` (Chambal, Gahirmatha, Mangalajodi, Rushikulya) were left untouched, as instructed — their lack of iNaturalist data is already honestly disclosed there

**v1.13 — Real favicon and app icons**
- Added the site's actual favicon set to `public/` (`favicon.ico`, 16×16/32×32 PNGs, Apple touch icon, and Android Chrome 192×192/512×512 icons) — the site previously had no favicon at all
- Wired them into `app/layout.tsx` via `Metadata.icons`/`Metadata.manifest`, and filled in `site.webmanifest`'s previously-blank `name`/`short_name` fields ("Wild India Atlas" / "Wild India") and set `theme_color` to the site's actual forest-900 brand color instead of the generator's default white

**v1.12 — Merged species list + Endemic/Iconic badges**
- New `scripts/tag-india-specialities.ts` (`npm run tag:specialities`) classifies every canonical species (21 Flagship + 1,268 Extended = 1,289 total) against iNaturalist, resolving India's `place_id` (6681) at runtime rather than hardcoding it, resolving a `taxonId` per species (reusing the one Extended fauna entries already carry from v1.10; resolving the rest via an exact scientific-name match), then batching `/v1/taxa/{ids}` lookups (30 per request) to read each species' `establishment_means` for India
- **Endemic: 71 yes / 176 no / 1,042 unknown.** "Unknown" is the default for anything without a real India listing — including, notably, the Flagship Asiatic Lion (`Panthera leo persica`), whose exact trinomial has no match in iNaturalist's taxonomy at all (only `Panthera leo leo` does) — flagged to `data/india-specialities-taxon-review.json` rather than guessed. **1 species total needed that ambiguous/no-match review this run.**
- Iconic is never inferred from observation counts — seeded `true` only for the 21 Flagship-curated species. `data/iconic-candidates.json` separately lists the top 15 Extended species per group (Bird/Mammal/Reptile/Amphibian, by summed observation count) as candidates for a human to review later, not as assigned tags
- `/species` is now a single merged, filterable, searchable list across both tiers — the old separate Flagship-grid-then-Extended-section split is gone. Group filter chips are computed dynamically from whatever values actually appear in the data (so Flagship's `Marine` category, which doesn't exist in Extended's Bird/Mammal/Reptile/Amphibian set, still gets a real chip instead of being miscategorized or dropped), plus new Endemic and Iconic toggle filters
- New `<SpecialityBadges>` shows on both list cards and profile pages (Flagship and Extended) whenever applicable — Endemic only for `endemic: "yes"` (never a visual state for "no"/"unknown"), Iconic only for `iconic: true`. Profile page content/structure is otherwise untouched, per the phase's scope
- Card layout is now consistent across tiers (photo if present, common + scientific name, group) — tier-specific depth (sighting difficulty, hotspot-confirmation counts, etc.) still lives only on the profile page, not the browsing card

**v1.11 — Extended species tier (merge + list + profile pages)**
- New `lib/extendedSpecies.ts` groups the real per-hotspot `data/ebirdSpecies.json` and `data/inaturalistSpecies.json` records into one canonical entry per species (keyed by scientific name), sitting alongside — not replacing — the 21 hand-curated `data/species.ts` (Flagship) entries
- **1,268 canonical Extended species** after dedup: 949 Bird, 122 Mammal, 139 Reptile, 58 Amphibian. 11 species that matched an existing Flagship entry were excluded from getting their own Extended page — matching is done on genus+species, not full-string equality, specifically so Flagship's trinomial subspecies names (e.g. Bengal Tiger's `Panthera tigris tigris`) correctly catch eBird/iNaturalist's binomial `Panthera tigris` as the same animal, rather than creating a redundant duplicate entry
- `/species` gains an "Extended species" section below the unchanged Flagship grid — searchable by name, filterable by group (Bird/Mammal/Reptile/Amphibian), with a "Show more" reveal pattern (mirrors the "Show all N species" toggle from v1.8's `<EbirdChecklist>`) rather than rendering 1,268 cards at once
- New auto-generated profile pages at `/species/[slug]` for Extended entries only (Flagship pages are untouched) — photo if present, common/scientific name, group, conservation status if present, source attribution ("via eBird" / "via iNaturalist"), and the full list of hotspots it's confirmed at, each linking to that hotspot's page. Deliberately has no viewingTips/photographyTips/similar-species sections, since that data doesn't exist for an auto-derived entry — no empty sections, no invented placeholder text
- No photo ever gets a stock/placeholder substitute, on cards or profile pages — a plain text card or a solid-color hero renders instead when `photoUrl` is absent (birds never have one; roughly a quarter of the fauna entries do)
- Build impact: 1,268 new static pages (1,338 total across the whole site, up from 70), generated in **under 20 seconds** locally — page count is real but doesn't look like an actual Vercel build-time concern at this scale, flagging it per the phase brief rather than assuming that silently

**v1.10 — iNaturalist species fetch (fauna)**
- New `scripts/fetch-inaturalist-species.ts` (`npm run fetch:inaturalist`) pulls real observed mammal/reptile/amphibian species for each of the 23 confirmed places in `data/inaturalistPlaces.ts`, via iNaturalist's `observations/species_counts` endpoint (no API key required), paginating past the 200-per-page limit where a place has more species than that
- Wrote `data/inaturalistSpecies.json`: **915 species records across 23 places** (Mammalia 421, Reptilia 364, Amphibia 130); 21 of the 23 places returned at least one species, 2 came back empty (Simlipal, Keibul Lamjao)
- Unlike eBird's `obsCount` (a single most-recent-checklist snapshot, deliberately downplayed in the UI in v1.8), iNaturalist's `observationCount` here is a real cumulative count per species at a place — a genuinely stronger signal, fine to treat as an actual relative-frequency indicator later. The two counts measure different things and the code comments call this out explicitly so a future pass doesn't conflate them if both end up rendered near each other
- `photoUrl` and `conservationStatus` are only ever included when iNaturalist's response actually has them — omitted, not fabricated or defaulted, when absent (most species had a photo; conservation status was present for 164 of the 915)
- Tooling only in this phase, same phasing discipline as the v1.5 eBird tooling and v1.9 iNaturalist discovery passes — nothing wired into the UI yet

**v1.9 — iNaturalist discovery tooling (fauna)**
- Mirrors the eBird discovery pattern from v1.5, but for iNaturalist and scoped to mammals/reptiles/amphibians for this phase — birds are already covered by eBird, flora/other taxa are future work
- New `scripts/find-inaturalist-places.ts` (`npm run find:inaturalist-places`) queries iNaturalist's public `places/autocomplete` endpoint (no API key required) for each of the 42 hotspots, trying a couple of reasonable name variants per park (stripping "National Park"/"Tiger Reserve"-style suffixes, splitting combined names like "Nagarhole / Kabini") before giving up, and writes candidates to `data/inaturalist-candidates.json` — 26 parks got exactly one candidate, 7 got multiple, 9 got no place match at all and are recorded as `method: "no-place-match"` rather than silently guessing a radius fallback
- New `data/inaturalistPlaces.ts` holds confirmed slug → iNaturalist place mappings, populated by hand after reviewing the candidates file — same conservative bar as `data/ebirdHotspots.ts`, and "single candidate" alone wasn't enough: three single-candidate results were excluded on closer look because the match itself was wrong (Manas matched Zimbabwe's Mana Pools National Park; Desert National Park matched Australia's Little Desert National Park) or unclear (Thattekad matched a generic "Forest division" administrative area, not clearly the sanctuary's own boundary) — 23 of 42 parks are confirmed, the rest wait for manual review
- Tooling only in this phase, consistent with how the eBird discovery pass shipped — no species-fetching script yet, and nothing rendered in the UI

**v1.8 — eBird checklist surfaced in the UI + structural-risk gap closed**
- New "Birds confirmed via eBird" section on the 36 hotspot pages that have a confirmed eBird mapping (`data/ebirdSpecies.json`) — additive to, and visually separate from, the existing curated `mainSpecies`/`birdSpecies` chips, not a replacement for them; collapses to the first 24 species with a "Show all N species" toggle since some parks report 300+
- Deliberately omits eBird's `obsCount` from the UI: it's the bird count from a single most-recent checklist, not a frequency or likelihood, and would overstate the signal if shown as a percentage-like figure — presence on the list is what's meaningful, not the count
- Curated species chips now carry a quiet cross-reference dot when that species also appears on the hotspot's eBird checklist (matched on common or scientific name) — a small signal, not a redesign of the chip UI
- `formatVerifiedDate` moved out of the client-only `<FreshnessBadge>` into `lib/formatDate.ts` so both the client checklist component and the server-rendered hotspot page can share one date formatter instead of each reimplementing it
- `data/structuralRisks.ts` gains three more entries — Hemis (remote, high-altitude, far less consistently documented online than lowland parks), Rushikulya Rookery (a seasonal, locally-coordinated nesting beach rather than a legally boundaried park), and Gahirmatha (a marine sanctuary with seasonally restricted boat access, distinct from Indravati's outright tourism closure) — closing the gap left by v1.5's eBird mapping pass, where these three (plus Chambal, Kuno, and already-flagged Indravati) had no candidate hotspot confident enough to map
- Two new `StructuralRisk` kinds — `sparse-documentation` and `restricted-marine-zone` — added because Hemis's and Gahirmatha's situations didn't genuinely fit the existing four; both render with the informational treatment, not Indravati's warning tone, since neither is a blanket access shutdown

**v1.7 — Structural-risk disclosures + re-verification cadence**
- New `data/structuralRisks.ts` flags five hotspots whose real complexity is structural (jurisdiction, authority, or access status), not seasonal, so squeezing them into the closures.ts shape would have understated what a visitor needs to know: Chambal River Sanctuary (spans 3 states, no single authority), Chilika Lake (managed by the Chilika Development Authority, not a Forest Department), Sundarbans (overlapping Forest Department / Biosphere Reserve / Tiger Reserve jurisdiction), Kuno (state Forest Department plus national Project Cheetah oversight), and Indravati (general tourism effectively closed for years on security grounds)
- New `<StructuralRiskNotice>` component — visually bigger and more prominent than `<FreshnessBadge>` on purpose, with a distinct warning treatment for Indravati's `access-restricted` flag versus an informational treatment for the three "jurisdiction is genuinely complicated" cases
- Wired in right after the hero on all five hotspot pages — above the fold, not buried near the closures section — so Indravati in particular states plainly, near the top of the page, that it isn't currently bookable for general tourism
- `/data-sources` gained a "Structural risk flags" section (with real examples rendered from the actual data) explaining what these flags mean, linked from every `<StructuralRiskNotice>`
- New `docs/verification-cadence.md` documents a quarterly review cadence for `unconfirmed` closures and all structural-risk entries, yearly for `official`/`inferred` closures with no structural flag — policy only in this phase, no automated checker yet
- Also backfilled a `VERSION_HISTORY.md` gap: the v1.6 entry below documents the `<FreshnessBadge>` popover redesign, which had shipped on `main` but was only ever noted as extra bullets under v1.4

**v1.6 — Interactive freshness badge + data-sources page**
- `<FreshnessBadge>` redesigned into a real `<button>`: the confidence label, source name, and a "what does this mean" explanation now reveal in an on-click/tap popover instead of living only in a hover `title` tooltip, which was invisible on mobile
- New `/data-sources` page explains the three confidence tiers in plain language, clarifies that "Verified" means when the note was last checked (not when the underlying policy changed), and that a source link is only ever shown once independently verified — linked from both the badge popover and a new sitewide footer
- A bare `<ConfidenceDot>` (color only, no text) now appears next to the location line on every hotspot card and map preview card — `HotspotCard` (both variants) and `HotspotPreviewCard` (both variants) — so the confidence signal is visible while browsing, not just on the detail page

**v1.5 — eBird species-data integration tooling**
- Groundwork for grounding `mainSpecies`/`birdSpecies` in real eBird citizen-science records instead of editorial judgment alone — this phase ships the discovery and fetch tooling, not new species content
- `scripts/find-ebird-hotspots.ts` (`npm run find:ebird-hotspots`) calls eBird's nearby-hotspots endpoint for each of the 42 hotspots and writes up to 5 candidate matches per park to a review file, `data/ebird-candidates.json` — it deliberately does not auto-pick a "best" candidate, since proximity alone doesn't prove an eBird hotspot sits inside a park's real boundary
- `data/ebirdHotspots.ts` holds the confirmed slug → eBird hotspot mappings once a human has reviewed the candidates — starts empty; nothing is auto-filled
- `scripts/fetch-ebird-species.ts` (`npm run fetch:ebird`) pulls the species list, taxonomy names, and a recent-observation count for each confirmed mapping into `data/ebirdSpecies.json`; slugs without a confirmed mapping are skipped and logged, not errored on
- Both scripts are manually-run CLI tools reading `EBIRD_API_TOKEN` from `.env.local` (see `.env.local.example`) — no live eBird calls happen at build or request time, consistent with this being a static site with no backend
- Hotspot detail pages show a small "via eBird · updated {month year}" note on the "What you can see" section only once real data exists for that park's slug — renders exactly as before everywhere else

**v1.4 — Provenance and freshness for closure data**
- `ClosureInfo` (`data/closures.ts`) now carries machine-readable provenance alongside each of the 42 researched closure facts: `sourceName` (the named department/authority, where the existing note already cited one — never invented), `lastVerified` (an ISO date), and a `confidence` rating (`official` / `inferred` / `unconfirmed`) derived from how firmly each note was already sourced
- `sourceUrl` is part of the type but intentionally left unset for this pass — no URL is fabricated; it's only filled in once a specific citable page has been verified
- New `<FreshnessBadge>` component renders a compact confidence-colored dot + "Verified {month year}" + source name (as a tooltip and secondary text) next to the closure note on hotspot detail pages, so a visitor can see at a glance how solid a given closure claim is
- No closure facts (`closesSeasonally`/`note`) were changed in this pass — this is a schema and UI layer on top of the existing v1.3 research, not new content

**v1.3 — Planning-oriented species guide + hotspot detail pages**
- New Species Guide (`/species`, `/species/[slug]`) covering 21 species with real, attributed Wikimedia Commons photography — where to actually see each species (derived live from hotspots' real species lists, not a hand-maintained list — this caught and fixed several factually wrong entries, e.g. Snow Leopard previously pointed at three central-India tiger forests instead of Hemis), best months, sighting difficulty, habitat, ethical viewing notes, photography tips, and similar-species cross-links
- 4 new hotspots added after verifying real range/species overlap first: Eravikulam National Park (Nilgiri Tahr), Singalila National Park (Red Panda), and Rushikulya + Gahirmatha (Olive Ridley Turtle nesting) — Gahirmatha's real access restrictions are prominently disclosed rather than glossed over
- 3 existing hotspots corrected with real, previously-missing species (Malabar Grey Hornbill at Thattekad, King Cobra at Sundarbans and Periyar, Lion-tailed Macaque at Periyar), found by checking official park records before assuming new hotspots were needed
- Hotspot detail pages rebuilt out to a full destination-page structure: unique SEO metadata + structured data per page (previously every page on the site shared one identical title/description), an ecosystem badge and "why go" callout in the hero, a structured experiences section, family-friendliness as an explicit fact, a season/closures block, a generic-but-personalized suggested itinerary, and a real geographic-nearest "nearby hotspots" section
- Species and hotspots now link to each other bidirectionally from their species/wildlife tags
- 14 more parks added to close the gap with a competitor audit (wildatlas.in/parks): Bandipur, Mudumalai, BRT Tiger Reserve, Sariska, Panna, Kuno, Rajaji, Melghat, Simlipal, Valmiki, Indravati, Namdapha, Keibul Lamjao, and Silent Valley — each fully sourced (real Wikimedia Commons photo, ecosystem classification, travel facts) to the same bar as the rest, bringing the atlas to 42 hotspots across 6 regions
- Added an Asiatic Cheetah species entry so Kuno's 2022 reintroduction story links properly from its hotspot page
- New "Species spotlight" section on hotspot detail pages — small clickable species cards (photo + name) for every mammal/bird with a matching species page
- Species pages' "where can I see this" hotspot cards switched to a compact style (photo, name, state only) now that the list can run to a dozen+ parks per species
- Homepage park/region counts are now computed live from the hotspot data instead of a hardcoded stat that had drifted out of date
- "Season & closures" on hotspot pages now shows a real, individually-researched closure fact per park (`data/closures.ts`) instead of the same generic per-season text for every hotspot — sourced from state forest department notices and official park portals, covering all 42 hotspots
- Backfilled `district`/Wikipedia/permit-portal links for the 14 newest hotspots to the same sourcing bar as the original 28

**v1.2 — Map as a trip-discovery engine**
- Region clustering: the map groups hotspots into clickable clusters by region at the default zoom, expanding into individual pins once you zoom into one
- Markers colored by ecosystem (forest, wetland, desert, alpine, mangrove, marine), manually classified from each hotspot's habitat description
- Multi-select map layer toggles (Mammals, Birds, Reptiles, Flora, Rare Species, Monsoon) that filter both clusters and pins
- Richer click-through preview: flagship species, difficulty, a linked permits badge, and a photography-friendly badge, alongside best months and nearest airport/railway
- Collapsible "Layers & legend" panel (collapsed by default) instead of a permanently-visible wall of chips
- Neutral state-fill color on the map so region, ecosystem, and "best this month" highlight colors no longer visually compete
- Homepage "Where to go in [month]" section: season-level field notes (best for / avoid / travel caution) alongside the existing data-driven "good bets" picks

**v1 — Prototype slice**
Initial map-first build: interactive India map with real state boundaries, 24 hotspot records with sourced photography and travel data, a filterable map/list explorer, and hotspot detail pages.
