"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { ArrowRight, AlertTriangle, Binoculars, Camera, Compass, ShieldAlert } from "lucide-react"; import { hotspots } from "@/data/hotspots"; import { SearchBar } from "@/components/SearchBar"; import { HotspotCard } from "@/components/HotspotCard"; import { IndiaMap } from "@/components/IndiaMap"; import { monthToSeason, seasonalWisdom } from "@/data/seasonalWisdom"; import { useState } from "react";
const chips=['Tiger','Birds','Rhino','Snow Leopard','Mangroves','Wetlands','Western Ghats']; const experiences=['Wildlife Photography','Birding','Safari','Monsoon Forests','Rare Species','Family Nature Trips'];
const monthAbbr=['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec']; const monthName=['January','February','March','April','May','June','July','August','September','October','November','December'];
export default function Home(){ const router=useRouter(); const [query,setQuery]=useState(''); const now=new Date(); const currentMonth=monthAbbr[now.getMonth()]; const currentSeason=monthToSeason[currentMonth]; const wisdom=seasonalWisdom[currentSeason]; const best=hotspots.filter(h=>h.bestMonths.includes(currentMonth)).slice(0,4); return <main><section className="relative flex min-h-[720px] items-center overflow-hidden px-4 pb-16 pt-28 sm:px-6 sm:pt-16 lg:min-h-[820px]"><div className="absolute inset-0 -z-20"><IndiaMap variant="hero" hotspots={hotspots} onSelect={h=>router.push("/map?query=" + encodeURIComponent(h.name))}/></div><div className="absolute inset-0 -z-10 bg-[linear-gradient(100deg,rgba(20,47,37,.96)_10%,rgba(20,47,37,.75)_45%,rgba(20,47,37,.35)_75%,rgba(20,47,37,.15)_100%),linear-gradient(0deg,rgba(20,47,37,.55),transparent_40%)]"/><div className="canopy-texture absolute inset-0 -z-10"/><div className="mx-auto w-full max-w-7xl"><div className="max-w-2xl text-white"><p className="flex items-center gap-3 font-mono text-xs font-semibold uppercase tracking-[0.25em] text-flare"><span className="h-px w-8 bg-flare"/>Field guide · Travel planner · Atlas</p><h1 className="mt-6 text-6xl font-semibold leading-[0.98] tracking-tight sm:text-7xl">Explore India&apos;s<br/><em className="text-flare not-italic">Wild Side</em></h1><p className="mt-6 max-w-lg text-lg leading-8 text-ivory/90">Plan wildlife, birding, and nature trips across India by species, season, region, and experience.</p><p className="mt-4 font-mono text-xs uppercase tracking-[0.15em] text-ivory/60">24 parks — 7 regions — live seasonal map</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/map" className="inline-flex items-center gap-2 rounded-sm bg-flare px-5 py-3 font-bold text-forest-900 transition hover:bg-white">Open Wildlife Map <ArrowRight size={18}/></Link><Link href={"/map?season=" + encodeURIComponent(currentSeason)} className="inline-flex items-center gap-2 rounded-sm border border-white/30 px-5 py-3 font-bold text-white transition hover:border-white hover:bg-white/10">Where to go this month</Link></div><div className="mt-8 max-w-xl"><SearchBar value={query} onChange={setQuery} placeholder="Search by species, park, state, season..." /></div><div className="mt-4 flex flex-wrap gap-2">{chips.map(c=><Link href={"/map?query=" + encodeURIComponent(c)} key={c} className="rounded-sm border border-white/25 px-3 py-1.5 font-mono text-xs uppercase tracking-wide text-white/80 transition hover:border-flare hover:text-flare">{c}</Link>)}</div></div></div></section><section className="mx-auto max-w-7xl px-4 py-14 sm:px-6"><div className="flex items-end justify-between gap-4"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.2em] text-river">{currentSeason} field notes</p><h2 className="text-3xl font-black text-forest-900">Where to go in {monthName[now.getMonth()]}</h2></div><Link href="/map" className="hidden font-bold text-forest-700 dark:text-forest-300 sm:inline-flex">View all</Link></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><div className="field-card rounded-sm p-4"><p className="flex items-center gap-2 font-mono text-xs font-bold uppercase tracking-wide text-forest-700 dark:text-forest-300"><Compass size={15}/>Best for</p><p className="mt-2 text-sm text-slate-700 dark:text-slate-300">{wisdom.bestFor}</p></div><div className="field-card rounded-sm p-4"><p className="flex items-center gap-2 font-mono text-xs font-bold uppercase tracking-wide text-clay"><AlertTriangle size={15}/>Avoid</p><p className="mt-2 text-sm text-slate-700 dark:text-slate-300">{wisdom.avoid}</p></div><div className="field-card rounded-sm p-4"><p className="flex items-center gap-2 font-mono text-xs font-bold uppercase tracking-wide text-amberfield"><ShieldAlert size={15}/>Travel caution</p><p className="mt-2 text-sm text-slate-700 dark:text-slate-300">{wisdom.travelCaution}</p></div></div><p className="mt-6 font-mono text-xs font-bold uppercase tracking-[0.2em] text-river">Good bets this month</p><div className="mt-3 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{best.map(h=><HotspotCard key={h.slug} hotspot={h}/>)}</div></section><section className="mx-auto max-w-7xl px-4 pb-16 sm:px-6" id="about"><h2 className="text-3xl font-black text-forest-900">Explore by experience</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{experiences.map((e,i)=><Link href={"/map?query=" + encodeURIComponent(e.split(' ')[0])} key={e} className="field-card rounded-sm p-5 transition hover:-translate-y-1"><div className="mb-4 grid h-11 w-11 place-items-center rounded-full bg-forest-100 text-forest-700 dark:bg-forest-900/50 dark:text-forest-300">{i%2?<Binoculars/>:<Camera/>}</div><h3 className="text-xl font-bold text-forest-900">{e}</h3><p className="mt-2 text-sm text-slate-700 dark:text-slate-300">Find locations, seasons, and planning notes matched to this travel style.</p></Link>)}</div></section></main>; }
