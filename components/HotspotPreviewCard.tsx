import Link from "next/link"; import { ArrowRight, CalendarDays, MapPin } from "lucide-react"; import type { Hotspot } from "@/data/types"; import { Tag } from "./Tag"; import { HotspotImage } from "./HotspotImage";
export function HotspotPreviewCard({ hotspot }: { hotspot: Hotspot }) { return <article className="field-card group overflow-hidden rounded-lg"><div className="relative"><HotspotImage slug={hotspot.slug} type={hotspot.type} className="h-44 w-full"/><div className="absolute inset-0 bg-gradient-to-t from-forest-900/75 via-forest-900/10 to-transparent"/><div className="absolute right-4 top-4"><Tag tone="sand">{hotspot.type}</Tag></div><div className="absolute bottom-4 left-4 text-white"><p className="flex items-center gap-1 text-sm font-semibold text-sand"><MapPin size={15}/>{hotspot.state} · {hotspot.region}</p><h2 className="mt-1 text-2xl font-semibold">{hotspot.name}</h2></div></div><div className="p-5"><p className="text-sm text-slate-700">{hotspot.summary}</p><div className="mt-4 flex flex-wrap gap-2">{hotspot.mainSpecies.slice(0,4).map(s=><Tag key={s}>{s}</Tag>)}{hotspot.experienceTags.slice(0,2).map(s=><Tag key={s} tone="blue">{s}</Tag>)}</div><p className="mt-4 flex items-center gap-2 text-sm font-semibold text-slate-700"><CalendarDays size={16}/>Best months: {hotspot.bestMonths.join(", ")}</p><Link href={"/hotspots/" + hotspot.slug} className="mt-4 inline-flex items-center gap-2 rounded-full bg-forest-700 px-4 py-2 text-sm font-bold text-white hover:bg-forest-900">View details <ArrowRight size={16}/></Link></div></article>; }
