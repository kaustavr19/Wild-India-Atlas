import Link from "next/link"; import { MapPin } from "lucide-react"; import type { Hotspot } from "@/data/types"; import { Tag } from "./Tag"; import { HotspotImage } from "./HotspotImage"; import { closureInfo } from "@/data/closures"; import { ConfidenceDot } from "./FreshnessBadge";
export function HotspotCard({ hotspot, active, onSelect, compact }: { hotspot: Hotspot; active?: boolean; onSelect?: () => void; compact?: boolean }) {
  const closure = closureInfo[hotspot.slug];
  const body = compact
  ? <article className={"field-card group flex items-center gap-3 overflow-hidden rounded-sm p-2 transition hover:-translate-y-0.5 " + (active ? "ring-2 ring-amberfield" : "")}><HotspotImage slug={hotspot.slug} type={hotspot.type} className="h-14 w-14 shrink-0 rounded-sm" showCredit={false}/><div className="min-w-0 flex-1"><h3 className="truncate font-bold text-forest-900">{hotspot.name}</h3><p className="mt-0.5 flex items-center gap-1 text-xs text-slate-600 dark:text-slate-400"><MapPin size={12} className="shrink-0"/><span className="truncate">{hotspot.state}</span>{closure && <ConfidenceDot confidence={closure.confidence} className="ml-1"/>}</p></div></article>
  : <article className={"field-card group overflow-hidden rounded-sm transition hover:-translate-y-0.5 hover:shadow-field " + (active ? "ring-2 ring-amberfield" : "")}><div className="relative"><HotspotImage slug={hotspot.slug} type={hotspot.type} className="h-36 w-full"/><div className="absolute right-3 top-3 rounded-sm bg-forest-900/90 px-2 py-1 font-mono text-[10px] font-semibold uppercase tracking-wider text-sand">{hotspot.type}</div></div><div className="p-4"><h3 className="font-bold text-forest-900">{hotspot.name}</h3><p className="mt-1 flex items-center gap-1 text-sm text-slate-600 dark:text-slate-400"><MapPin size={14}/>{hotspot.state}{closure && <ConfidenceDot confidence={closure.confidence} className="ml-1"/>}</p><p className="mt-3 line-clamp-2 text-sm text-slate-700 dark:text-slate-300">{hotspot.summary}</p><div className="mt-3 flex flex-wrap gap-2">{hotspot.mainSpecies.slice(0,3).map(s=><Tag key={s}>{s}</Tag>)}</div><p className="mt-3 text-xs font-semibold uppercase tracking-wide text-slate-500 dark:text-slate-400">Best: {hotspot.bestMonths.slice(0,5).join(", ")}</p></div></article>;
  return onSelect ? <button className="w-full min-w-0 text-left" onClick={onSelect}>{body}</button> : <Link className="block min-w-0" href={"/hotspots/" + hotspot.slug}>{body}</Link>; }
