import Link from "next/link"; import { MapPin } from "lucide-react"; import type { Hotspot } from "@/data/types"; import { Tag } from "./Tag"; import { HotspotImage } from "./HotspotImage";
export function HotspotCard({ hotspot, active, onSelect }: { hotspot: Hotspot; active?: boolean; onSelect?: () => void }) { const body = <article className={"field-card group overflow-hidden rounded-lg transition hover:-translate-y-0.5 hover:shadow-field " + (active ? "ring-2 ring-amberfield" : "")}><div className="relative"><HotspotImage slug={hotspot.slug} type={hotspot.type} className="h-36 w-full"/><div className="absolute right-3 top-3"><Tag tone="sand">{hotspot.type}</Tag></div></div><div className="p-4"><h3 className="font-bold text-forest-900">{hotspot.name}</h3><p className="mt-1 flex items-center gap-1 text-sm text-slate-600"><MapPin size={14}/>{hotspot.state}</p><p className="mt-3 line-clamp-2 text-sm text-slate-700">{hotspot.summary}</p><div className="mt-3 flex flex-wrap gap-2">{hotspot.mainSpecies.slice(0,3).map(s=><Tag key={s}>{s}</Tag>)}</div><p className="mt-3 text-xs font-semibold uppercase tracking-wide text-slate-500">Best: {hotspot.bestMonths.slice(0,5).join(", ")}</p></div></article>; return onSelect ? <button className="w-full text-left" onClick={onSelect}>{body}</button> : <Link href={"/hotspots/" + hotspot.slug}>{body}</Link>; }
