import { Clock, ExternalLink, Landmark, MapPin, Plane, ShieldCheck, Train, Users } from "lucide-react"; import type { Hotspot } from "@/data/types"; import { permitPortalUrl } from "@/data/officialLinks"; import { district as districtBySlug } from "@/data/accessPoints";
function mapsUrl(place: string) { return "https://www.google.com/maps/search/?api=1&query=" + encodeURIComponent(place); }
export function QuickFactsCard({ hotspot }: { hotspot: Hotspot }) { const facts = [{icon:Clock,label:'Best time',value:hotspot.bestMonths.join(', ')},{icon:Clock,label:'Ideal duration',value:hotspot.idealDuration},{icon:ShieldCheck,label:'Difficulty',value:hotspot.difficulty},{icon:Users,label:'Family-friendly',value:hotspot.experienceTags.includes('Family-friendly') ? 'Yes' : 'No'},...(districtBySlug[hotspot.slug] ? [{icon:MapPin,label:'District',value:districtBySlug[hotspot.slug]}] : []),{icon:Landmark,label:'Permits',value:hotspot.permitRequired,href:permitPortalUrl[hotspot.slug]},{icon:Plane,label:'Airport',value:hotspot.nearestAirport,href:mapsUrl(hotspot.nearestAirport)},{icon:Train,label:'Railway',value:hotspot.nearestRailway,href:mapsUrl(hotspot.nearestRailway)}]; return <aside className="field-card rounded-sm p-5"><h2 className="text-lg font-bold text-forest-900">Quick facts</h2><div className="mt-4 grid gap-4">{facts.map(({icon:Icon,label,value,href})=><div key={label} className="flex gap-3"><Icon className="mt-1 text-forest-700 dark:text-forest-300" size={18}/><div><p className="text-xs font-bold uppercase text-slate-500 dark:text-slate-400">{label}</p>{href ? <a href={href} target="_blank" rel="noopener noreferrer" className="inline-flex items-center gap-1 text-sm font-semibold text-river hover:underline dark:text-sky-300">{value}<ExternalLink size={12}/></a> : <p className="text-sm font-semibold text-slate-800 dark:text-slate-200">{value}</p>}</div></div>)}</div></aside>; }
